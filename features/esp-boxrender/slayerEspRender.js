import { EntityArmorStand } from '../../misc/entities';
import Settings from '../../config';
// use renderlibv2
const targetEntity = "Dummy";

register("renderOverlay", () => {
    if (Settings.slayerEspBox) {
        
    };
});
console.log('Skaro >>> Slayer ESP Loaded.');

//                    let name = stand.getName().removeFormatting();
                    // Checks hp name tag to spawned by name tag to check whos boss
  //                  if (name.includes(`Spawned by: ${Player.getName()}`) && (name.includes('Horror') || name.includes('Packmaster') || name.includes('Broodfather') || name.includes('Seraph') || name.includes('Demonlord') || name.includes('Bloodfiend'))) {
                        // render esp box
     //               }


     